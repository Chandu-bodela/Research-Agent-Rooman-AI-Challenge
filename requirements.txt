# --- Frontend ---------------------------------------------------------- #
streamlit>=1.36.0
streamlit-extras>=0.4.3
plotly>=5.22.0

# --- Core / utils -------------------------------------------------------- #
python-dotenv>=1.0.1
pydantic>=2.7.0
numpy>=1.26.4
tqdm>=4.66.4

# --- Document parsing ----------------------------------------------------- #
PyMuPDF>=1.24.5          # PDF text + page-level extraction
python-docx>=1.1.2       # DOCX parsing
markdown>=3.6            # Markdown -> plain text/html

# --- Embeddings + Vector store -------------------------------------------- #
sentence-transformers>=3.0.1
faiss-cpu>=1.8.0
# transformers imports torchvision for some image-aware models; install it explicitly
# so the app can start cleanly in fresh environments.
torchvision>=0.20.0

# --- LLM providers (install only what you use; all are optional) --------- #
openai>=1.35.0
anthropic>=0.30.0
google-generativeai>=0.7.0
groq>=0.9.0

# --- Database -------------------------------------------------------------- #
# sqlite3 ships with Python; no package needed.

# --- Export ---------------------------------------------------------------- #
fpdf2>=2.7.9              # Lightweight PDF export (no system deps)
markdown2>=2.4.13

# --- Optional bonus features ------------------------------------------------ #
pytesseract>=0.3.10        # OCR (requires system Tesseract binary)
Pillow>=10.3.0
SpeechRecognition>=3.10.4  # Voice question transcription (optional)
